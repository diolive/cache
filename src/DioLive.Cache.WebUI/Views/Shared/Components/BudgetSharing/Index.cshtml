@model BudgetSharingsVM

@foreach (ShareVM share in Model.Shares)
{
    @await Html.PartialAsync("Components/BudgetSharing/ShareItem", share)
}

@await Html.PartialAsync("Components/BudgetSharing/NewShare", new ShareVM { BudgetId = Model.BudgetId })